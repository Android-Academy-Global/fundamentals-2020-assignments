<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.android.academy.fundamentals">

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.FundamentalsAssignments">

        <activity android:name=".MainActivity">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <!-- TODO Solution: Add a "SecondActivity" class into the manifest. Like this: -->
<!--        <activity android:name=".MainActivity_Solution" />-->
<!--        <activity android:name=".SecondActivity_Solution" />-->

    </application>

</manifest>
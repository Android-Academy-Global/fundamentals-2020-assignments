<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <!--
        This "res-solution" folder is synthetic.
        It can not be created with Android Studio tools.
        It's also ignored during the compile time.
        This isn't counted as a resource.

        It's a wrong naming.
        It's created only to show a TODOs result.
    -->

    <!-- TODO 3: Extract to resources -->
    <!-- TODO 3: see also "values -> strings -> id:hello_world -->
    <!-- TODO 5: add id "first_activity_text_view" for the TextView -->

    <TextView
        android:id="@+id/first_activity_text_view"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/hello_world"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>
